Flask==3.1.2
Flask-CORS==6.0.0
Flask-Limiter==3.8.0
faster-whisper==1.2.0
deep-translator==1.11.4
# Pinned, and the pin needs a date on it: YouTube changes its player and its
# throttling every few weeks, and a yt-dlp that is a few months old starts
# answering "HTTP Error 403: Forbidden" on every download. This pin was
# 2025.10.14 and had gone ten months stale — every YouTube job failed while the
# code was fine. Verified at the time of this bump by downloading a real video
# with the app's own format selector. If downloads start failing again, check
# this line's age BEFORE looking for a bug.
yt-dlp==2026.08.19
requests==2.32.4
python-dotenv==1.1.1
celery==5.4.0
redis==5.2.1
gunicorn==23.0.0
openai==1.35.13
google-genai>=1.47.0  # New Google Gemini SDK (replaces deprecated google-generativeai)

Werkzeug==3.1.3
google-api-python-client==2.154.0
ffmpeg-python==0.2.0
kombu==5.4.2
billiard==4.2.1
structlog==24.4.0
tiktoken==0.8.0  # Phase A+: Precise token counting for OpenAI
browser-cookie3==0.20.1  # Fix YouTube 403 errors
# psutil: /health uses Process().create_time() to detect a process running
# stale code. It was documented as "optional" — but without it the fallback
# reports the CURRENT time as the process start, so the staleness check always
# answered "fresh". A detector that lies is worse than none.
psutil==7.2.2
